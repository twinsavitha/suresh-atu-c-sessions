// Compile this program with the -Wall option to get the hint.
#include <iostream>
using std::cout;

const int MAX_INDEX = 10;
int main()
{
	for (int nIdx = 0; nIdx < MAX_INDEX; nIdx++)
	{
		if (nIdx = 5)
		{
			cout << "Value is " << nIdx << '\n';
		}
	}
	return(0);
}


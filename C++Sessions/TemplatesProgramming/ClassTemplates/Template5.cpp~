// Demonstrating the shorthand notation and full template parameter listing
#include <iostream>
using std::cout;
using std::endl;

template <typename T>
class CNode
{
	T data;
	T *pNext;
};

template <typename U>
class CLinkedList
{
public:
	
private:
	CNode<U> *pHead; // Need to specify the parameter list
};
int main()
{
	return(0);
}
